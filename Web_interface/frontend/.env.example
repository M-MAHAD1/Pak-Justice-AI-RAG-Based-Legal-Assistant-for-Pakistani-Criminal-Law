# Frontend environment variables (Vite)
# Base URL of your Express server.
# Local dev:
VITE_API_BASE_URL=http://localhost:3000

# If you deploy the frontend behind the same domain as the server,
# you can set this to an empty string and use a relative path instead.
# VITE_API_BASE_URL=
